# لوحة الزوّار (/admin)

صفحة محمية بكلمة مرور تعرض من دخل الموقع. الرابط موجود في فوتر الموقع باسم «الزوّار».

## ما الذي يُسجَّل لكل زيارة

الوقت · عنوان IP · الدولة/المنطقة/المدينة (تقريبية من الـIP، تعمل على Vercel فقط) ·
المتصفح والجهاز (user-agent) · من أين جاء الزائر (referrer) · الصفحة · لغة المتصفح.

> لا يمكن معرفة اسم الزائر — فقط هذه البيانات التقنية.

## التخزين

MongoDB Atlas (مضبوط عبر تكامل Vercel، مجاني). قاعدة `portfolio`، مجموعة `visits`.

## متغيّرات البيئة

| المتغيّر | المصدر |
|---|---|
| `ADMIN_PASSWORD` | كلمة مرور من اختيارك — تُضاف يدويًا في Vercel |
| `MONGODB_URI` | يضيفه تكامل MongoDB Atlas تلقائيًا في Vercel |
| `MONGODB_DB` | اختياري، الافتراضي `portfolio` |

محليًا: نفس القيم في ملف `.env.local`.

## الاستخدام

افتح `/admin` → أدخل كلمة المرور → جدول بآخر 500 زيارة + إجمالي العدد وعدد الزوّار المختلفين (حسب IP).
الجلسة تبقى 7 أيام عبر كوكي، وزيارات صفحة `/admin` نفسها لا تُسجَّل.

## الملفات

- `components/VisitTracker.js` — يرسل نبضة عند كل فتح صفحة
- `app/api/track/route.js` — يخزّن الزيارة في MongoDB
- `app/api/admin/login/route.js` — تحقّق كلمة المرور + كوكي جلسة
- `app/api/admin/visits/route.js` — يرجّع الزيارات بعد التحقق
- `app/admin/page.js` — واجهة الدخول والجدول
- `lib/mongo.js` · `lib/auth.js` — أدوات مساعدة
